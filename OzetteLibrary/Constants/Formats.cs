namespace OzetteLibrary.Constants
{
    /// <summary>
    /// A constants class for string formats.
    /// </summary>
    public static class Formats
    {
        /// <summary>
        /// The string format for looking up a net credential username.
        /// </summary>
        public const string NetCredentialUserNameKeyLookup = "NetCredentialUser-{0}";

        /// <summary>
        /// The string format for looking up a net credential password.
        /// </summary>
        public const string NetCredentialUserPasswordKeyLookup = "NetCredentialPassword-{0}";
    }
}
